# Product

Career Loop is the daily job-search heartbeat: it scans live job boards, keeps a private list on your Mac, and shows you a short digest of what’s new — without asking you to create developer accounts.

**Stranger default = class A + B only** (bundled runtime + your OS login / notifications). No Resend, GitHub Actions, Vercel, or AI API keys on the first-run path.
